We can get them to run this command to do all of the set up (install the python extension and module, clone the repo and open vscode)

code --install-extension ms-python.python
    && pip install pygame
    && git clone https://github.com/AlexanderFair/cmscode.git
    && code cmscode
